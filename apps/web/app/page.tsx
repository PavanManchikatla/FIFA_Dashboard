import HoloLatticeLanding from '@/components/HoloLatticeLanding';

// Landing (PLAN.md experience #1): the cinematic react-three-fiber holo lattice — dot-matrix
// field + 16 stadium light-pillars (amber when live) + bloom, with a cross-fade into the map.
export default function Home() {
  return <HoloLatticeLanding />;
}
